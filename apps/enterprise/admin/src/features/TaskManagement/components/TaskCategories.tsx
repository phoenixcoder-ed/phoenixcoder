import React from 'react';

import { Box, Typography } from '@mui/material';

const TaskCategories: React.FC = () => {
  return (
    <Box>
      <Typography variant="h4">分类管理</Typography>
      <Typography>分类管理页面开发中...</Typography>
    </Box>
  );
};

export default TaskCategories;
