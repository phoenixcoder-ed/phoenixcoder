import React from 'react';

import { Box, Typography } from '@mui/material';

const TaskDetail: React.FC = () => {
  return (
    <Box>
      <Typography variant="h4">任务详情</Typography>
      <Typography>任务详情页面开发中...</Typography>
    </Box>
  );
};

export default TaskDetail;
