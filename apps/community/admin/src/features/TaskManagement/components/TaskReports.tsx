import React from 'react';

import { Box, Typography } from '@mui/material';

const TaskReports: React.FC = () => {
  return (
    <Box>
      <Typography variant="h4">报告统计</Typography>
      <Typography>报告统计页面开发中...</Typography>
    </Box>
  );
};

export default TaskReports;
